package com.abi.excelreader

 class ExcelData {
     var name: String = ""
         get() = field        // getter
         set(value) {         // setter
             field = value
         }

     var age: Int = 0
         get() = field        // getter
         set(value) {         // setter
             field = value
         }

     var course: String = ""
         get() = field        // getter
         set(value) {         // setter
             field = value
         }

     var track: String = ""
         get() = field        // getter
         set(value) {         // setter
             field = value
         }

     var grade: Int = 0
         get() = field        // getter
         set(value) {         // setter
             field = value
         }

     var location: String = ""
         get() = field        // getter
         set(value) {         // setter
             field = value
         }
 }

